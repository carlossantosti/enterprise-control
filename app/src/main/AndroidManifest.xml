<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.carlosti.enterprise">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="br.com.carlosti.enterprise.ProductActivity"
            android:label="@string/product" />
        <activity
            android:name="br.com.carlosti.enterprise.EnterpriseActivity"
            android:label="@string/enterprise" />
        <activity
            android:name="br.com.carlosti.enterprise.ListEnterpriseActivity"
            android:label="@string/enterprises" />
        <activity
            android:name="br.com.carlosti.enterprise.ListProductsActivity"
            android:label="@string/list_products" />
        <activity
            android:name="br.com.carlosti.enterprise.ChooseEnterpriseActivity"
            android:theme="@style/NoActionBarTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>